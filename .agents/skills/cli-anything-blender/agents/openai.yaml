interface:
  display_name: "Blender CLI Workflow"
  short_description: "Use the repo Blender CLI for 3D workflows"
  default_prompt: "Use $cli-anything-blender to create, edit, render, preview, or process a Blender scene through the repository CLI."

policy:
  allow_implicit_invocation: true
